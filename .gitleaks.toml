# gitleaks allowlist — server-nexe
# Tots els findings auditats i confirmats com a false positives (2026-05-09).
# Cap secret real al repositori.

[allowlist]
description = "False positives confirmats per auditoria de seguretat 2026-05-09"
paths = [
  # tests/ — cobreix tests/ arrel i plugins/**/tests/ (substring match)
  "tests/",

  # Documentació pública — placeholders: YOUR_API_KEY, el-teu-token, etc.
  "knowledge/",

  # Codi denylist d'uploads — capçaleres PEM com a patrons de detecció, no claus reals
  "plugins/web_ui_module/api/routes_files.py",

  # Catàleg instal·lador — model IDs Qwen (qwen35_122b_moe, qwen35_397b_moe)
  "installer/installer_catalog_data.py",
  "installer/swift-wizard/Resources/models.json",
]
