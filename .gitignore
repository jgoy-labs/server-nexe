# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node
node_modules
dist
dist-ssr
*.local

# Vite
.vite/

# Env (secrets i configs locals)
.env
.env.*
!.env.example

# Editor / IDE
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/

# macOS
.DS_Store
._*

# Build outputs
target/
*.dmg
*.AppImage

# Tauri externalBin sidecar (regenerat per scripts/build-sidecar.sh)
src-tauri/binaries/
# Sidecar bundle tarball (regenerat per scripts/pre-bundle-sidecar.sh)
src-tauri/sidecar-bundle.tar.gz
# SHA-256 del tarball — generat al pre-bundle, llegit pel runtime
# (sidecar.rs) per detectar re-builds dins la mateixa CARGO_PKG_VERSION.
src-tauri/sidecar-bundle.sha256

# Internal audit artifacts (DEV-only, not for OSS)
.muthur/

# Python (si s'afegeix sidecar amb venv local)
__pycache__/
*.pyc
.venv/
venv/
.coverage
.coverage.*
htmlcov/

# Internal vault docs — NEVER push to OSS repo (DEV-only)
20*_closeout_*.md
20*_auditoria_*.md
DIARI-*.md
diari/
index_nexe-app.md
README-nexe-app.md
SOURCE_OF_TRUTH.md
LLICONS_APRESES.md
consultoria.md
nexe-app.PREV-META-*/

# F5.6 — sidecar runtime dirs (regenerats al primer launch dev/Tauri)
storage/
personality/
src-tauri/storage/
src-tauri/personality/
