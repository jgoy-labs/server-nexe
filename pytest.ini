[pytest]
# Nexe Server - Pytest Configuration
asyncio_mode = auto

# Test markers
markers =
    unit: Unit tests (fast, mocked dependencies)
    integration: Integration tests (require real services like Ollama)
    e2e: End-to-end tests (full system, slow)
    slow: Slow tests (skip with -m "not slow")
    gpu: Tests that require a GPU backend (Ollama, MLX or llama.cpp)

# Default options
addopts = -v --tb=short --cov --cov-report=term-missing -m "not integration and not gpu"

# Test discovery
testpaths =
    core
    memory
    personality
    plugins
    tests

python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Ignore warnings from dependencies
filterwarnings =
    ignore::DeprecationWarning:pydantic.*
    ignore::DeprecationWarning:qdrant_client.*
    ignore::UserWarning:sentence_transformers.*
