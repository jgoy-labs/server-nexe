# ─────────────────────────────────────────────────────────────
# Nexe Server 0.9 - Dependencies
# Minimal & Secure
# ─────────────────────────────────────────────────────────────

# --- Core Server ---
fastapi==0.135.3
uvicorn[standard]==0.44.0
pydantic==2.12.5
pydantic-settings>=2.4,<3.0
python-multipart==0.0.26
starlette-csrf==3.0.0
httpx==0.28.1
qdrant-client==1.17.1
toml==0.10.2
structlog==25.5.0
click==8.3.2
rich==15.0.0
typer==0.24.1
python-dotenv==1.2.2
pyyaml>=6.0.2
numpy==2.4.4
slowapi==0.1.9
prometheus-client==0.25.0
tenacity==9.1.4
psutil==7.2.2
huggingface_hub==1.10.1
# macOS-only deps (tray app) → requirements-macos.txt

# --- Memory & RAG ---
fastembed>=0.3.6
pypdf>=6.9.2  # PDF support for knowledge ingestion (CVE-2026-31826, CVE-2026-33123, CVE-2026-33699 fixed)

# --- Encryption at rest ---
cryptography>=44.0.0
keyring>=25.0.0
sqlcipher3>=0.5.0

# --- Testing ---
pytest>=8.0.0,<10.0.0
pytest-asyncio>=0.23.0,<2.0.0
pytest-cov>=5.0.0,<8.0.0

# --- Inference Engines (Apple Silicon / MLX) ---
# Note: Install only on Mac ARM64 — handled dynamically by install_nexe.py
# mlx-lm==0.30.7        # Text LLMs (pinned: first version with qwen3_5 support)
# mlx-vlm==0.1.27       # Multimodal VLM (compatible amb mlx-lm 0.30.7 i transformers 4.57+)

# --- Inference Engines (Llama.cpp / GGUF) ---
# llama-cpp-python  # Installed dynamically with GPU flags by install_nexe.py
