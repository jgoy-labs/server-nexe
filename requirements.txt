# ─────────────────────────────────────────────────────────────
# Nexe Server 0.9 - Dependencies
# Minimal & Secure
# ─────────────────────────────────────────────────────────────

# --- Core Server ---
fastapi==0.136.3
starlette==1.2.1  # explicit pin for CVE-2025-54121 / CVE-2025-62727
uvicorn[standard]==0.48.0
pydantic==2.13.4
pydantic-settings==2.14.1
python-multipart==0.0.29  # CVE-2026-40347; GHSA-pp6c-gr5w-3c5g fixed in 0.0.27
starlette-csrf==3.0.0
httpx==0.28.1
qdrant-client==1.18.0
toml==0.10.2
structlog==25.5.0
click==8.4.1
rich==15.0.0
typer==0.26.4
python-dotenv==1.2.2  # GHSA-mf9w-mj56-hr94 symlink attack fixed in 1.2.2
pyyaml==6.0.3
# numpy: transitiva MLX/opencv; >=2 requerit per mlx-vlm>=0.4.4 via opencv-python>=4.12 [refresh 2026-05-16]
numpy==2.4.6
slowapi==0.1.9
prometheus-client==0.25.0
tenacity==9.1.4
psutil==7.2.2
# huggingface_hub: transitiva mlx-lm/fastembed; transformers 5.x requereix >=1.5.0;
# F2.A11 va intentar pinar a 1.15.0 però pip-audit detecta conflicte amb typer==0.26.4
# (deps transitives click). Mantenim >=1.5.0 (warn dep-pinning acceptat — deferit
# a F5 wheels quan revisem requirements split macOS/general). [refresh 2026-05-17]
huggingface_hub>=1.17.0
filelock==3.29.0  # GHSA-qmgc-5h2g-mvrw GHSA-w853-jp5j-5j7f TOCTOU fixed in 3.20.3 (dep transitiva explicitada)
platformdirs>=4.10.0,<5  # cross-platform user data dirs (Linux/macOS); used by core.onboarding_state and installer.install_headless

# --- Memory & RAG ---
fastembed==0.8.0
pypdf==6.12.2  # PDF support for knowledge ingestion; GHSA-3crg-w4f6-42mx GHSA-4pxv-j86v-mhcw GHSA-7gw9-cf7v-778f GHSA-jj6c-8h6c-hppx GHSA-x284-j5p8-9c5p fixed in 6.10.0

# --- Encryption at rest ---
cryptography==48.0.0
keyring==25.7.0
sqlcipher3==0.6.2

# --- Testing ---
pytest==9.0.3  # CVE-2025-71176
pytest-asyncio==1.4.0
pytest-cov==7.1.0

# --- Inference Engines (Llama.cpp / GGUF) ---
# llama-cpp-python  # Installed dynamically with GPU flags by install_nexe.py

# Note: MLX inference engine (Apple Silicon) is in requirements-macos.txt
