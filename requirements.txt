# ─────────────────────────────────────────────────────────────
# Nexe Server 0.8 - Dependencies
# Minimal & Secure
# ─────────────────────────────────────────────────────────────

# --- Core Server ---
fastapi==0.135.1
uvicorn[standard]==0.42.0
pydantic==2.12.5
python-multipart==0.0.22
starlette-csrf==3.0.0
colorama==0.4.6
tomli==2.0.1; python_version < "3.11"
httpx==0.28.1
qdrant-client==1.17.1
toml==0.10.2
structlog==25.5.0
click==8.3.1
rich==14.3.3
typer==0.24.1
python-dotenv==1.2.2
pyyaml==6.0.3
numpy==2.4.3
slowapi==0.1.9
prometheus-client==0.24.1
tenacity==9.1.4
psutil==7.2.2
rumps==0.4.0
huggingface_hub==1.7.2

# --- Memory & RAG ---
sentence-transformers>=4.0.0,<6.0.0
pypdf==6.9.1  # PDF support for knowledge ingestion (security fixes)

# --- Testing ---
pytest>=8.0.0,<10.0.0
pytest-asyncio>=0.23.0,<2.0.0
pytest-cov>=5.0.0,<8.0.0

# --- Inference Engines (Apple Silicon / MLX) ---
# Note: Install only on Mac ARM64
# mlx-lm  # Installed dynamically by install_nexe.py if hardware detected

# --- Inference Engines (Llama.cpp / GGUF) ---
# llama-cpp-python  # Installed dynamically with GPU flags by install_nexe.py
