# ─────────────────────────────────────────────────────────────
# Nexe Server 0.9 - Dependencies
# Minimal & Secure
# ─────────────────────────────────────────────────────────────

# --- Core Server ---
fastapi==0.136.1
starlette>=1.0.0,<2.0.0  # explicit pin for CVE-2025-54121 / CVE-2025-62727
uvicorn[standard]==0.46.0
pydantic==2.13.3
pydantic-settings>=2.14.0,<3.0
python-multipart==0.0.26  # CVE-2026-40347
starlette-csrf==3.0.0
httpx==0.28.1
qdrant-client==1.17.1
toml==0.10.2
structlog==25.5.0
click==8.3.3
rich==15.0.0
typer==0.25.0
python-dotenv==1.2.2
pyyaml>=6.0.3
numpy==2.4.4
slowapi==0.1.9
prometheus-client==0.25.0
tenacity==9.1.4
psutil==7.2.2
huggingface_hub==1.12.0
# macOS-only deps (tray app) → requirements-macos.txt

# --- Memory & RAG ---
fastembed>=0.8.0
pypdf>=6.10.2  # PDF support for knowledge ingestion (CVE-2026-31826, CVE-2026-33123, CVE-2026-33699 fixed)

# --- Encryption at rest ---
cryptography>=47.0.0
keyring>=25.7.0
sqlcipher3>=0.6.2

# --- Testing ---
pytest>=9.0.3,<10.0.0  # CVE-2025-71176
pytest-asyncio>=1.3.0,<2.0.0
pytest-cov>=7.1.0,<8.0.0

# --- Inference Engines (Apple Silicon / MLX) ---
# Note: Install only on Mac ARM64 — handled dynamically by install_nexe.py
# mlx-lm==0.31.2        # Text LLMs (compatible amb mlx-vlm 0.4.x)
# mlx-vlm==0.4.4        # Multimodal VLM (gemma4, qwen3_5_moe, qwen3_vl, llava, …)

# --- Inference Engines (Llama.cpp / GGUF) ---
# llama-cpp-python  # Installed dynamically with GPU flags by install_nexe.py
