# ─────────────────────────────────────────────────────────────
# Nexe Server 0.8 - Dependencies
# Minimal & Secure
# ─────────────────────────────────────────────────────────────

# --- Core Server ---
fastapi==0.128.8
uvicorn[standard]==0.34.3
pydantic==2.10.6
python-multipart==0.0.22
starlette-csrf==3.0.0
colorama==0.4.6
tomli==2.0.1; python_version < "3.11"
httpx==0.27.2
qdrant-client==1.16.1
toml==0.10.2
structlog==25.5.0
click==8.1.7
rich==13.7.0
typer==0.9.0
python-dotenv==1.2.1
pyyaml==6.0.1
numpy==1.26.4
slowapi==0.1.9
prometheus-client==0.19.0
tenacity==9.1.2
psutil==5.9.8
huggingface_hub==0.36.2

# --- Memory & RAG ---
sentence-transformers==5.1.2
pypdf==6.7.5  # PDF support for knowledge ingestion (security fixes)

# --- Testing ---
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
pytest-cov>=5.0.0,<6.0.0

# --- Inference Engines (Apple Silicon / MLX) ---
# Note: Install only on Mac ARM64
# mlx-lm  # Installed dynamically by install_nexe.py if hardware detected

# --- Inference Engines (Llama.cpp / GGUF) ---
# llama-cpp-python  # Installed dynamically with GPU flags by install_nexe.py
