# ─────────────────────────────────────────────────────────────
# Nexe Server 0.9 - Dependencies
# Minimal & Secure
# ─────────────────────────────────────────────────────────────

# --- Core Server ---
fastapi==0.128.8
starlette>=0.49.1,<1.0.0  # explicit pin for CVE-2025-54121 / CVE-2025-62727
uvicorn[standard]==0.34.3
pydantic==2.10.6
pydantic-settings>=2.4,<3.0
python-multipart==0.0.20  # CVE GHSA-pp6c-gr5w-3c5g (DoS) — màx versió compatible Python 3.9; patch requereix >=3.10
starlette-csrf==3.0.0
httpx==0.27.2
qdrant-client==1.16.1
toml==0.10.2
structlog==25.5.0
click==8.1.7
rich==13.7.0
typer==0.9.0
python-dotenv==1.2.1  # CVE GHSA-mf9w-mj56-hr94 (symlink attack) — sense patch disponible a PyPI 2026-05-07
pyyaml>=6.0.2
numpy==1.26.4
slowapi==0.1.9
prometheus-client==0.19.0
tenacity==9.1.2
psutil==5.9.8
huggingface_hub==0.36.2
filelock>=3.19.1  # GHSA-qmgc-5h2g-mvrw GHSA-w853-jp5j-5j7f TOCTOU — 3.20.3 no existeix a PyPI (màx 3.19.1); CVEs pendents
# macOS-only deps (tray app) → requirements-macos.txt

# --- Memory & RAG ---
fastembed>=0.3.6
pypdf>=6.10.0  # PDF support for knowledge ingestion; GHSA-3crg-w4f6-42mx GHSA-4pxv-j86v-mhcw GHSA-7gw9-cf7v-778f GHSA-jj6c-8h6c-hppx GHSA-x284-j5p8-9c5p fixed in 6.10.0

# --- Encryption at rest ---
cryptography>=44.0.0
keyring>=25.0.0
sqlcipher3>=0.5.0

# --- Testing ---
pytest>=9.0.3,<10.0.0  # CVE-2025-71176
pytest-asyncio>=1.0.0,<2.0.0
pytest-cov>=7.0.0,<8.0.0

# --- Inference Engines (Apple Silicon / MLX) ---
# Note: Install only on Mac ARM64 — handled dynamically by install_nexe.py
# mlx-lm==0.31.2        # Text LLMs (compatible amb mlx-vlm 0.4.x)
# mlx-vlm==0.4.4        # Multimodal VLM (gemma4, qwen3_5_moe, qwen3_vl, llava, …)

# --- Inference Engines (Llama.cpp / GGUF) ---
# llama-cpp-python  # Installed dynamically with GPU flags by install_nexe.py
