# Ollama macOS bundle checksums — pinned for offline DMG build (B8 r4)
#
# Format: <sha256-hex>  <relative-filename>   (consumed by `shasum -a 256 -c`)
#
# Source URL (primary):   https://ollama.com/download/Ollama-darwin.zip
#                         (redirects to GitHub releases/latest/download/Ollama-darwin.zip)
# Source URL (secondary): https://github.com/ollama/ollama/releases/download/<version>/Ollama-darwin.zip
# Source of truth:        https://github.com/ollama/ollama/releases/download/<version>/sha256sum.txt
#                         (published and signed by the Ollama maintainers at release time)
#
# Last updated:           2026-05-16 (B8 bump v0.22.1 → v0.24.0)
# Ollama version pinned:  v0.24.0 (latest, 2026-05-16)
# File size:              167608636 bytes (~160 MB)
#
# Cross-validation performed at pin time (2026-05-01 13:46):
#   1. Downloaded zip via ollama.com (latest CDN redirect)        → SHA_A
#   2. Downloaded zip via GitHub releases/download/v0.22.1/...    → SHA_B
#   3. Fetched sha256sum.txt published by Ollama at the release   → SHA_OFFICIAL
#   4. SHA_A == SHA_B == SHA_OFFICIAL  ✓ (binary diff also clean)
#
# Bump history:
#   2026-05-01 02:22  v0.22.0 pinned (a410e2f722fb…b278)  — initial B8 landing
#   2026-05-01 13:50  v0.22.1 pinned (387536bd3462…065b)  — auto-bump after first
#                                                            real-world block at
#                                                            DMG build (B8 fix
#                                                            caught upstream rev,
#                                                            triple cross-val OK)
#   2026-05-16        v0.24.0 pinned (8073624ec798…99b1)  — bump: upstream release.
#                                                            Cross-val: CDN + GitHub
#                                                            sha256sum.txt ✓ match
#
# Update procedure (when Ollama releases a new version):
#   1. Verify the new release tag at https://github.com/ollama/ollama/releases
#   2. Fetch the official sha256sum.txt for that tag:
#         curl -fsSL https://github.com/ollama/ollama/releases/download/<TAG>/sha256sum.txt
#      and read the SHA256 for `./Ollama-darwin.zip` → call it SHA_OFFICIAL
#   3. Download the zip from BOTH sources and compute SHA locally:
#         curl -fsSL -o /tmp/A.zip https://ollama.com/download/Ollama-darwin.zip
#         curl -fsSL -o /tmp/B.zip https://github.com/ollama/ollama/releases/download/<TAG>/Ollama-darwin.zip
#         shasum -a 256 /tmp/A.zip /tmp/B.zip
#   4. If SHA(/tmp/A.zip) == SHA(/tmp/B.zip) == SHA_OFFICIAL → safe to pin
#      (replace the line below; bump `Last updated` and `Ollama version pinned`).
#   5. If ANY of the three diverge → STOP. Possible MITM, CDN cache divergence,
#      or upstream tampering. Verify from a clean network (different ISP / VPN /
#      tethering) before committing the new pin. Do NOT bypass.
#   6. As an additional safety net for future reviewers, you may also run:
#         codesign --verify --deep --strict <extracted Ollama.app>
#      to confirm Apple Developer ID signature integrity.
#
# ── pinned checksum (one entry per line, format: <sha256>  <filename>) ──
8073624ec7986f9259f14a1234f5a5818f6285767f08b18cd0fbb4d1136599b1  Ollama-darwin.zip
